spring:
  application:
    name: payment-prod
  jackson:
    property-naming-strategy: SNAKE_CASE
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    listener:
      ack-mode: record
      missing-topics-fatal: false
    consumer:
      group-id: payment-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
        spring.json.trusted.packages: "kr.magicbox.payment.adapter.in.kafka.event"
        spring.json.type.mapping: payment-approve:kr.magicbox.payment.adapter.in.kafka.event.PaymentApproveCommandEvent,payment-cancel:kr.magicbox.payment.adapter.in.kafka.event.PaymentCancelCommandEvent
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      topic:
        attempts: 5
        backoff:
          delay: 2s
          max-delay: 30s
  datasource:
    url: ${MYSQL_URL}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

security:
  trusted:
    ips:
      - ${TRUSTED_IP_GATEWAY}

inbox:
  max-event-age-minutes: 60

toss:
  payment:
    secret-key: ${TOSS_SECRET_KEY}
    base-url: https://api.tosspayments.com

resilience4j:
  circuitbreaker:
    instances:
      toss-payment:
        sliding-window-size: 20
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5
